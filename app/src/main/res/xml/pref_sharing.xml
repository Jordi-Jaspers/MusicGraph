<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/pref_sharing_titles"
        android:entryValues="@array/pref_sharing_values"
        android:key="sharing"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_standard_share" />

</PreferenceScreen>
